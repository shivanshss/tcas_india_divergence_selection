# Configuration file for genomic analysis step 1

# Number of threads for parallel processing
num_threads=4

# Path to the reference genome
reference_genome=/path/to/reference_genome.fa

# Paths to software and tools
bwa=/path/to/bwa
picard_jar=/path/to/picard.jar
gatk=/path/to/gatk
samtools=/path/to/samtools
annovar=/path/to/annovar/table_annovar.pl

# ANNOVAR database directory
annovar_db=/path/to/annovar_db

# Genome build version # not required if you have a single/final reference genome (commented out)
#genome_build=your_genome_build_version

# Sample information
sample_info=(
    ["sample1"]="sample1_R1.fastq" "sample1_R2.fastq"
    ["sample2"]="sample2_R1.fastq" "sample2_R2.fastq"
    # Add more sample entries as needed
)



# Run the analysis
./[bash_script].sh [this_file]
